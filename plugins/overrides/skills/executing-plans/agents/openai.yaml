interface:
  display_name: "Executing Plans"
  short_description: "Execute written plans with checkpoints"
  default_prompt: "Use $executing-plans to work through this implementation plan with verification checkpoints."
